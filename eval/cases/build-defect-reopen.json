{
  "name": "build-defect-reopen",
  "description": "Case A — a defect in work already marked done is a status correction: the affected rows are reopened and fixed under them, and no row is appended.",
  "fixture": "build-defect",
  "phase": 1,
  "prompt": "mano-build — requiring src/release-stage.js returns base+release, not base+feature+release",
  "assertions": [
    "build_reopened_instead_of_appending",
    "build_wrote_no_story_files"
  ]
}
