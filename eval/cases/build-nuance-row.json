{
  "name": "build-nuance-row",
  "description": "Invocation case C — an in-goal nuance passed as `mano build \"<text>\"` is appended as a `+N` row carrying the user's own words verbatim, linked to the Exit Criterion it changes, and no existing row is rewritten.",
  "fixture": "build-stage-chain",
  "phase": 1,
  "prompt": "mano-build \"compose the feature label from the base module's value rather than hard-coding the word base\"",
  "assertions": [
    "build_appended_the_users_words",
    "build_wrote_no_story_files"
  ]
}
